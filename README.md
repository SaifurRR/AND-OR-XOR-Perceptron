# AND-OR-XOR Gate - Perceptron

2 input Gates

AND - > Test Score = 1.0
OR  - > Test Score = 1.0
XOR - > Test Score = 0.5

XOR can't be represented with a 'Linear Classifier' as the 4 possible outputs for 2 inputs can't be separated with a single straight line.

